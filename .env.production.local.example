# Copia a .env.production.local y pon tus URLs reales de Render (Dashboard → servicio → URL).
# NO commitear .env.production.local (está en .gitignore).

API_BASE_URL=https://TU-BACKEND.onrender.com
SOCKET_URL=https://TU-GATEWAY.onrender.com
