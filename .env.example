# Flutter app environment (copy to .env)
API_BASE_URL=http://localhost:3000
SOCKET_URL=http://localhost:3000
MONGODB_URI=mongodb://localhost:27017/smartmedic

# Probar paciente + médico a la vez (web):
# - Backend: cd backend && pnpm run dev  (puerto 3000, compartido por ambos)
# - Dos pestañas Flutter en el MISMO puerto: cada pestaña tiene su login (sessionStorage)
# - O dos navegadores distintos (Chrome + Firefox)
# - Tras este cambio: cierra sesión en cada pestaña y vuelve a iniciar sesión
